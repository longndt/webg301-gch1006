{% extends "base.html.twig" %}

{% block body %}
<div class="container col-md-8 text-center mt-3">
   <div class="row">
      <div class="col-4">
         <div class="card">
            <img src="{{ author.image }}" width="300" height="300">
         </div>
      </div>
      <div class="col-6">
         <table class="table table-bordered">
            <tr>
               <th colspan="2" class="bg-light">
                  <h2 class="text-danger"> Author Information </h2>
               </th>
            </tr>
            <tr class="h4">
               <th>Name</th>
               <td>{{ author.name }}</td>
            </tr>
            <tr class="h4">
               <th>Age </th>
               <td>{{ author.age }} </td>
            </tr>
            <tr class="h4">
               <th>Address</th>
               <td>{{ author.address }}</td>
            </tr>
            <tr class="h4">
               <th class="col-md-4">Published Books</th>
               <td class="col-md-8">
               {% if author.books != null %}
                  {% for book in author.books %}
                     <a href="{{ path('book_detail', {'id' : book.id })}}">
                        {{ book.title }} 
                     </a>
                     <br>
                  {% endfor %}
               {% endif %}
               </td>
            </tr>
         </table>
      </div>
   </div>
</div>
{% endblock %}